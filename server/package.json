{
  "name": "ai-commit-server",
  "version": "0.1.0",
  "description": "Thin proxy that keeps the Gemini API key server-side. Optional 'hosted' backend for the ai-commit CLI. Deployable on Render's free tier.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
